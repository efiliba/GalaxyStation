namespace GalaxyStation
{
    public class AtmosphereTest
    {
//        private Atmosphere gas;

/*        public void TestGas0AtStart()
        {
            gas = new Atmosphere();            
            int effect = gas.Effect(0);

            if (effect != 0)
                throw new System.Exception("Gas not 0");
        }

        public void TestGas100AtTime1Distance0()
        {
            gas = new Atmosphere();
            System.Threading.Thread.Sleep(1000);
            int effect = gas.Effect(0);

            if (effect != 100)
                throw new System.Exception("Gas not 100");
        }

        public void TestGas0AtTime1Distance1()
        {
            gas = new Atmosphere();
            System.Threading.Thread.Sleep(1000);
            int effect = gas.Effect(1);

            if (effect != 0)
                throw new System.Exception("Gas not 0");
        }

        public void TestGas0AtTime1Distance2()
        {
            gas = new Atmosphere();
            System.Threading.Thread.Sleep(1000);
            int effect = gas.Effect(2);

            if (effect != 0)
                throw new System.Exception("Gas not 0");
        }

        public void TestGas100AtTime5Distance0()
        {
            gas = new Atmosphere();
            System.Threading.Thread.Sleep(5000);
            int effect = gas.Effect(0);

            if (effect != 100)
                throw new System.Exception("Gas not 100");
        }

        public void TestGas0AtTime5Distance100()
        {
            gas = new Atmosphere();
            System.Threading.Thread.Sleep(5000);
            int effect = gas.Effect(100);

            if (effect != 0)
                throw new System.Exception("Gas not 0");
        }

        public void TestGas0AtTime100Distance0()
        {
            gas = new Atmosphere();

            int effect = gas.Effect(0);

            if (effect != 0)
                throw new System.Exception("Gas not 0");
        }*/

    }
}
